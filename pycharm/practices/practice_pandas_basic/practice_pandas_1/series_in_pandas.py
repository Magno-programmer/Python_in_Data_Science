import pandas as pd

name_cars = ["Gol", "Chevrolet", "Fiat Uno", "Palio"]


"""               A Series in library pandas               """

# In a series the output will be the order of the list in columns
# and for each index going to exist a value
print(pd.Series(name_cars))
